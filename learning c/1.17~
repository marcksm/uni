#include<stdio.h>
#include<stdlib.h>

int main(){
int dec, bin, result, aux;

	printf("Digite um numero com base decimal\n");
	scanf("%d", &dec);
	while (dec>0);
	aux=dec%2;	
	dec=dec/2;
	if (aux == 0){
	result=aux;
	result=1;
	}
	if (aux == 1){
	result=0;
	}
	printf("%d ",result);
}
