<%= form_with(model: professor, local: true) do |form| %>
  <div class="form-group">
    <%= form.label :email %>
    <%= form.email_field :email, class: "form-control", autofocus: "autofocus" %>
  </div>

  <%= form.submit "Send Invititation", class: "btn  btn-default" %>
<% end %>
